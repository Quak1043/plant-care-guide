{
  "name": "plant-care-guide",
  "version": "8.0.0",
  "private": true,
  "description": "绿植养护指南 —— 静态站点源码与构建脚本（零第三方依赖）",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "assets": "python _build/optimize_images.py && python _build/fetch_fonts.py",
    "plants": "python _build/extract_plants.py",
    "dev": "node --watch build.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
